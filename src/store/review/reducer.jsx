import { normalizedReviews } from "../../constants/normalized-fixtures";

const initialState = normalizedReviews.reduce()

const reviewReducer = (state, action) => {
switch (action.type) {
    default:
        return state;
}
}

export default reviewReducer;